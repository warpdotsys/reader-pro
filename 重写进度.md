# JAR Kotlin 重写进度

## 当前状态

- 审计纠正：移除了目标 JAR 中不存在的 `User.kt`；`VertExt.kt` 已按 JAR 恢复为 Vert.x 的 `success`、`error` 与 `globalHandler` 扩展，目录定位改由 JAR 的 `ReaderAdapterHelper` 提供。

- 依据：`reader-pro-3.2.14.jar`、Kotlin Metadata 与对应 Smali。
- 原则：逐文件恢复，不根据名称或现有实现猜测字节码未证明的行为。
- 已完成：186 个文件。
- 重建基线：已移除所有未重写源码；旧实现保存在 `deprecated/pre-jar-rewrite` 分支。
- 语言归属：已按 JAR `kotlin/Metadata` 与社区版本路径纠正 Java/Kotlin 边界；epublib、umdlib、TTS 与相关工具类保持 Java 源文件。社区版本仅用于路径与语言交叉校验，行为仍以 JAR/Smali 为准。
- 质量整改：不再提交反编译器的原始 Java 输出。Java 文件先以经 JAR/Smali 对账的社区源码结构恢复泛型、常量引用、初始化位置、注解与文档；JAR 有差异时再手工补齐差异。Kotlin 文件同样以 Metadata/Smali 验证，而非把 Java 反编译结果改写成 Kotlin。
- 质量整改批次：已恢复 54 个既有 Java 类型的源级结构，并新增 4 个 EPUB 浏览导航 Java 类型；每批独立通过 Build 后合并。
- CI：Build 使用 `--warning-mode all`，Kotlin 警告视为错误，Java 启用 `-Xlint:all`；已加入 JUnit 回归测试覆盖日期、UMD Unicode、UTF-8 BOM 与 EPUB 媒体类型。

## 已完成

| 文件 | 状态 | 验证 |
| --- | --- | --- |
| 第十八批基础工具与独立实体（10 个文件） | 已重写 | 按 JAR 手写恢复 `FileUtils`（含 SortType 与 4 个比较器）、`StringUtils`（中文数字/相对日期/全半角）、`ZipUtils`（含协程挂起压缩）、`MapDeserializerDoubleAsIntFix`、`AppConfig`、`Debug`、`FileExtensions` 与 `BookGroup`/`ReplaceRule`/`TxtTocRule`；修正旧批次 BookGroup 缺 `cover`、ReplaceRule 缺 `scopeTitle/scopeContent/timeoutMillisecond` 的偏差；字符串常量经常量池逐一核实，中文按 `\uXXXX` 转义；闭包零新增依赖。 |
| 第十七批 Rhino 脚本引擎栈（18 个 Java 文件 + 1 个边界纠正） | 已重写 | 按 JAR 内置 `rhino-1.7.13-1.jar` 恢复 `com.script` API（Bindings/ScriptContext/ScriptEngine 等 10 个）与 `com.script.javascript` 实现（8 个）；`RhinoScriptEngine` 从此前误用的 `javax.script` 边界纠正为 JAR 的 `com.script` 实现；逐文件对账字节码并清理泛型/反编译伪影，中文异常消息按 `\uXXXX` 恢复；`javac --release 17` 编译零错误。 |
| 第十六批规则解析核心（10 个文件） | 已重写 | 恢复变量容器、平衡规则切分和 JSONPath/JSoup/Regex/XPath 解析器；补齐 JAR 的 `StringExtensions` 与 `JsoupXpath` 引擎依赖，PR 前完成包路径、语言归属、依赖子闭包和差异审计。 |
| 第十五批 HTTP 编码执行链（10 个文件） | 已重写 | 保持 ICU 字符集识别器为 Java，恢复 HTML 元信息和统计探测；`OkHttpUtils` 恢复重试、可取消 await、BOM 处理、请求体与表单构造分支，并新增 HTML charset 回归测试。 |
| 第十四批 HTTP 基础传输层（7 个文件） | 已重写 | 以 JAR 而非社区旧版的数据库占位实现恢复命名空间 `CookieStore`；同时恢复 cookie 协议、网络 URL/IPv4 工具、Retrofit 协程适配器、SSL 参数和代理客户端缓存。 |
| 第十三批缓存与适配器边界（11 个文件） | 已重写 | `CacheManager` 按 JAR 恢复为带用户命名空间的实例类，保留空 key、TTL、二进制与 `QueryTTF` 分支；同步恢复默认适配器、适配器替换入口、`DebugLog` 与 `StrResponse`，并新增默认目录和合成 HTTP 响应回归测试。 |
| 第十三批 6 个缓存基础类型 | 已重写 | 保留 `getWorkDir` 的工作目录与 macOS 分支，恢复 `appCtx`、缓存实体、文件缓存与 `CacheManager`，为 `BaseSource` 的登录缓存逻辑提供依赖闭包。 |
| `QueryTTF.java` | 已重写 | 使用同包上游源码结构，逐项对照 JAR 的 14 个内部类型、cmap/glyf 分支和公开查询 API；为书源缓存闭包提供 TTF 解析能力。 |
| 第十二批 13 个书源基础类型 | 已重写 | 常量、Base64/加密/JSON、Cookie 与书源规则模型均按 JAR Kotlin Metadata 保持原始 Java/Kotlin 归属；为 `BookSource` 兼容反序列化闭包铺设基础。 |
| 第十一批 10 个 EPUB 读写管线类型 | 已重写 | `EpubReader/EpubWriter`、NCX、OPF 读写与资源加载器均保持 Java；JAR Kotlin Metadata、社区路径和依赖闭合检查通过。 |
| `src/main/kotlin/com/htmake/reader/api/ReturnData.kt` | 已重写 | 与 `ReturnData.class`、`ReturnData.smali` 对照；Kotlin 编译通过。 |
| `src/main/kotlin/io/legado/app/exception/*.kt` | 已重写 | `NoStackTraceException`、三个子类与 `ConcurrentException` 均按 JAR/Smali 的构造器、继承关系和栈追踪行为恢复。 |
| `src/main/kotlin/io/legado/app/help/http/RequestMethod.kt` | 已重写 | 恢复 `GET`、`POST` 枚举值。 |
| `src/main/kotlin/io/legado/app/constant/Status.kt` | 已重写 | 恢复 `STOP=0`、`PLAY=1`、`PAUSE=3` 常量。 |
| `src/main/kotlin/io/legado/app/utils/LogUtils.kt` | 已重写 | 恢复 `Throwable.printOnDebug()` 扩展函数。 |
| `src/main/kotlin/com/htmake/reader/lib/tts/exceptions/TtsException.kt` | 已重写 | 恢复私有构造器与 Java 可调用的静态工厂。 |
| 第二批 12 个低耦合类型 | 已重写 | 已逐个确认 JAR class 存在；覆盖 Spring 事件、Gson/Retrofit 转换器、TTS 枚举、常量和值对象、异常与 BOM 工具。 |
| 第三批 10 个低耦合类型 | 已重写 | 已逐个确认 JAR class 存在；覆盖 EPUB manifest 类型、流包装器、文本/反射工具、Gson 反序列化和 RSS/动作常量。 |
| 第四批 10 个 UMD/EPUB 基础类型 | 已重写 | 直接依据 JAR 的 UMD 写入链恢复流计数、字节序、压缩分块、书籍/章节/封面/头尾结构。 |
| 第五批 20 个 EPUB 资源与编码类型 | 已重写 | 已逐个确认 JAR class 存在；恢复资源/引用/惰性加载、媒体类型、流代理、BOM 探测与 XML/HTTP 编码判定的原始分支。 |
| 第六批 10 个资源集合与基础类型 | 已重写 | 已逐个确认 JAR class 存在；恢复 EPUB 资源集合、书脊、指南、目录、标识符、日期、集合工具、数据类与 TTS 常量。 |
| 第七批 10 个 EPUB 书籍与处理类型 | 已重写 | 已逐个确认 JAR class 存在；恢复完整 Relator 枚举、作者/元数据/书籍聚合、处理管线、DOM/XML 支持与资源工具。 |
| 第八批 10 个 TTS 与实体类型 | 已重写 | 依据 JAR/Smali 恢复语音枚举、SSML/配置/WebSocket 服务、许可证与用户实体、编码表和书源生成器。 |

## 待处理

后续按依赖顺序每批处理 10-20 个文件。每批将更新本表、创建 PR、等待 Build；构建通过后合并，再进入下一批。

## 自动化

- `.github/workflows/build.yml` 只执行 `clean test`。
- Docker workflow 暂存至 `.github/workflows-disabled/docker.yml`，在整个项目重写完成前不会触发。
